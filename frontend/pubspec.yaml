name: hospital_finance_frontend
description: "Hospital Finance Dashboard – Flutter Web"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter
  fl_chart: ^0.66.0

  cupertino_icons: ^1.0.8
  http: ^1.2.2
  shared_preferences: ^2.3.2
  flutter_secure_storage: ^10.0.0
  flutter_localizations:
    sdk: flutter
  pdf: ^3.11.3
  csv: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
